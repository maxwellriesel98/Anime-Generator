<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anime Details</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body class="bodyColor">
    <% if(locals.chosenAnime) { %>
    <div class="px-4 py-5 my-5 text-center">
        
        <h1 class="display-5 fw-bold textColorHeading">
            <%= chosenAnime.title %> <% if(chosenAnime.title_english !== null){ %>
            or <%= chosenAnime.title_english %> <% } %></h1>

        <img class="d-block mx-auto mb-4" src="<%= chosenAnime.images.jpg.image_url %>" 
        alt="image of <%= chosenAnime.title_english %>">

        <div class="col-lg-6 mx-auto">
          <p class="textColorBody mb-4">Aired <%= chosenAnime.aired.string %></p>
          <p class="textColorBody mb-4"><%= chosenAnime.synopsis %></p>
          <p class="textColorBody mb-4">Viewer Score: <% if(chosenAnime.score === null){ %> 
              No Scores Given <% } else { %>
              <%= chosenAnime.score %><% } %></p>
          <p class="textColorBody mb-4">Rated by <% if(chosenAnime.scored_by === null){ %> 
              nobody <% } else { %>
              <%= chosenAnime.scored_by %>  users<% } %></p>
          <p class="textColorBody mb-4"><% if(chosenAnime.type === "Movie"){ %>
              <%= chosenAnime.duration %> Long<% } else {%>
              <%= chosenAnime.episodes %> Episodes with a length of <%=chosenAnime.duration%>isode<%}  %>
      
          </p>
          <% } else { %>
              <h1 class="display-5 fw-bold text-body-emphasis">Sorry! No Anime match your filters</h1> <% } %>
          
          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <form action="/" method="GET" class="mb-3">
                <input type="submit" value="Go Back" class="btn btn-light btn-lg px-4 gap-3">
            </form>
          </div>
        </div>
      </div>







    
    <h1></h1>

    
   
    

    

</body>
</html>